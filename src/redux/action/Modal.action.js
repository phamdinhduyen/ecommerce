import { createAction } from "@reduxjs/toolkit";
export const updateStateModal = createAction("UPDATE_MODAL_STATE");
export const updateStateModalRegister = createAction(
  "UPDATE_MODAL_STATE_REGISTER"
);
